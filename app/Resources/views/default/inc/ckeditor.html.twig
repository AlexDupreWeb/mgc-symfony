{% block javascripts %}
<script src="{{ asset('ckeditor/ckeditor.js') }}"></script>

<script type="text/javascript">
    $(function () {
        $('.ckeditor-default').each(function(){
            var cke_config = "{{ asset('ckeditor/config-default-fr.js') }}";

            if($(this).attr('data-cke-config-language') == 'en'){
                cke_config = "{{ asset('ckeditor/config-default-en.js') }}";
            }

            CKEDITOR.replace( $(this).attr('id'),{
                customConfig: cke_config,

                filebrowserBrowseUrl : '{{ asset('responsive_filemanager/filemanager/dialog.php') }}?type=2&editor=ckeditor&fldr=',
                filebrowserUploadUrl : '{{ asset('responsive_filemanager/filemanager/dialog.php') }}?type=2&editor=ckeditor&fldr=',
                filebrowserImageBrowseUrl : '{{ asset('responsive_filemanager/filemanager/dialog.php') }}?type=1&editor=ckeditor&fldr='
            });
        });
    });
</script>
{% endblock %}